<div class=container>
<h1>Burger Madness</h1>

<h2>Burgers to be devoured</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <div></div>
			{{burger_name}}

			<button data-id="{{id}}">
					DEVOUR IT!
			</button>

		

    {{/unless}}
  {{/each}}
</ul>

<h2>Too Late !!! These burgers have been devoured</h2>

<ul>
  	{{#each burgers}}
    	{{#if devoured}}
      	{{burger_name}}
    	{{/if}}
 	{{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="b_name">Give Your Burger a Name:</label>
    <input type="text" id="b_name" name="burger_name">
  </div>

  
  <button type="submit">Add It!</button>
</form>

</div>
